<h2 class="text-center"><%= t ".rooms" %></h2>
<div class="pull-left btn-add-cost">
  <%= link_to "  Tạo mới", new_admin_room_path,
    class: "fa fa-plus btn btn-default" %>
</div>
<div id="table-floors">
  <table class="table table-hover new-order table-bordered">
    <thead>
      <tr class="info row">
        <th class="col-md-2"><%= t ".order" %></th>
        <th class="col-md-8"><%= t ".name_floor" %></th>
        <th class="col-md-2"></th>
      </tr>
    </thead>
    <tbody>
      <% @rooms.each_with_index do |room, index| %>
        <tr class="row">
          <td class="col-md-2"><%= index + 1 %></td>
          <td class="col-md-8"><%= room.name %></td>
          <td class="col-md-2">
            <%= link_to edit_admin_room_path(room),
              class: "btn btn-default button-1" do %>
              <i class="fa fa-wrench"><%= t ".edit_room" %></i>
            <% end %>
          </td>
        </tr>
      <% end %>
      <%= paginate @rooms %>
    </tbody>
  </table>
</div>
